{
  "name": "quick-connect",
  "version": "1.0.0",
  "dependencies": {
    "jayson": "^2.0.4",
    "sequelize": "^4.8.4",
    "sequelize-cli": "^3.0.0-3",
    "sqlite3": "^3.1.11"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "scripts": {
    "build": "babel --presets es2015 src --out-dir lib --ignore test.js",
    "watch": "yarn build -- --watch",
    "start": "yarn build && node lib/server.js"
  }
}
